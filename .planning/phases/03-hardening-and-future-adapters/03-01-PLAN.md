---
phase: 03-hardening-and-future-adapters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - textnorm/bench_test.go
  - textnorm/fuzz_test.go
  - textnorm/doc.go
autonomous: true
requirements:
  - future-only
must_haves:
  truths:
    - "Hot-path text normalization is measured on representative corpora before any optimization work starts."
    - "Malformed text and repeated normalization do not panic or drift on the public `textnorm` APIs."
    - "Package docs show how to run benchmarks and make the future adapter boundary explicit."
    - "Streaming adapters remain intentionally deferred until real usage proves they are worth the surface area."
  artifacts:
    - path: "textnorm/bench_test.go"
      provides: "Benchmark coverage for core pipelines and presets"
    - path: "textnorm/fuzz_test.go"
      provides: "Fuzz targets for malformed input and idempotence-sensitive behavior"
    - path: "textnorm/doc.go"
      provides: "Updated package docs, examples, and adapter-boundary guidance"
  key_links:
    - from: "textnorm/bench_test.go"
      to: "textnorm/pipeline.go"
      via: "Benchmarks call the existing fluent pipeline constructors instead of inventing new hot-path helpers"
      pattern: 'Benchmark.*New\(\)'
    - from: "textnorm/fuzz_test.go"
      to: "textnorm/presets.go"
      via: "Fuzz targets exercise SearchPreset(), CanonicalPreset(), and DBSafePreset() end to end"
      pattern: "Fuzz.*Preset"
    - from: "textnorm/doc.go"
      to: ".planning/research/SUMMARY.md"
      via: "Docs reflect the research-backed decision to keep streaming adapters optional and deferred"
      pattern: "streaming adapters.*defer"
---

<objective>
Harden the `textnorm` package with representative benchmarks, fuzz coverage, and docs that keep the library-only boundary explicit.

Purpose: Measure the current shape before optimizing, lock in malformed-input behavior, and keep future adapter work clearly out of the core API.
Output: Benchmark and fuzz test files plus updated package docs that explain the hardening scope.
</objective>

<execution_context>
@/Users/alessiosavi/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/alessiosavi/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md
@.planning/phases/01-core-pipeline-and-unicode-safety/01-01-SUMMARY.md
@.planning/phases/02-token-pipelines-and-presets/02-01-SUMMARY.md
@.planning/phases/02-token-pipelines-and-presets/02-02-SUMMARY.md
@textnorm/pipeline.go
@textnorm/presets.go
@textnorm/tokens.go
@textnorm/doc.go
@textnorm/regression_test.go

<interfaces>
From textnorm/pipeline.go:
```go
type Pipeline struct {
    stages []Stage
}

func New() Pipeline
func (p Pipeline) Then(stage Stage) Pipeline
func (p Pipeline) Run(input string) (string, error)
```

From textnorm/tokens.go:
```go
type TokenPipeline struct {
    source Pipeline
    stages []TokenStage
}

func (p Pipeline) SplitTokens() TokenPipeline
func (tp TokenPipeline) MapTokens(fn func(string) string) TokenPipeline
func (tp TokenPipeline) FilterTokens(fn func(string) bool) TokenPipeline
func (tp TokenPipeline) JoinTokens(sep string) Pipeline
```

From textnorm/presets.go:
```go
func WithWidthFold() PresetOption
func SearchPreset(opts ...PresetOption) Pipeline
func CanonicalPreset(opts ...PresetOption) Pipeline
func DBSafePreset(opts ...PresetOption) Pipeline
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add benchmark coverage for core and preset pipelines</name>
  <files>textnorm/bench_test.go</files>
  <read_first>textnorm/pipeline.go, textnorm/presets.go, textnorm/tokens.go, .planning/phases/02-token-pipelines-and-presets/02-02-SUMMARY.md</read_first>
  <action>Create benchmark functions for representative corpora so Phase 3 measures the current cost of normalization before any optimization is attempted. Cover at least these cases: a no-op/ASCII input, a mixed-Unicode search-key input, and a dirty DB-safe input with invalid UTF-8 or NULs represented as string data. Use the public `textnorm` constructors only, call `b.ReportAllocs()`, and keep the benchmark table explicit with subtests so future regressions can compare like-for-like results.</action>
  <verify><automated>go test ./textnorm -run '^$' -bench 'Benchmark(Pipeline|SearchPreset|CanonicalPreset|DBSafePreset)' -benchmem -count=1</automated></verify>
  <done>Benchmark coverage exists for representative inputs and reports allocation data for the current pipeline/preset paths.</done>
</task>

<task type="auto">
  <name>Task 2: Add fuzz coverage for malformed input and stability</name>
  <files>textnorm/fuzz_test.go</files>
  <read_first>textnorm/pipeline.go, textnorm/presets.go, textnorm/regression_test.go, .planning/research/PITFALLS.md</read_first>
  <action>Add fuzz targets that exercise the core pipeline and the main presets against malformed text, combining marks, emoji, repeated whitespace, and invalid UTF-8/NUL byte sequences converted into strings. Seed the fuzzers with the edge cases called out in research, and assert that the public APIs stay panic-free while idempotence-sensitive paths remain stable when run twice. Keep the fuzzers focused on published entry points; do not reach into unexported helpers or add new normalization logic.</action>
  <verify><automated>go test ./textnorm -run '^$' -fuzz 'Fuzz(Pipeline|SearchPreset|CanonicalPreset|DBSafePreset)' -fuzztime=1s -count=1</automated></verify>
  <done>Fuzz coverage exists for malformed input and catches stability regressions in the exported APIs.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh package docs for hardening scope and adapter boundary</name>
  <files>textnorm/doc.go</files>
  <read_first>textnorm/pipeline.go, textnorm/presets.go, textnorm/bench_test.go, textnorm/fuzz_test.go, .planning/research/SUMMARY.md</read_first>
  <action>Rewrite the package docs so they match the hardened phase-3 surface: explain the benchmark and fuzz entry points, keep the current preset examples accurate, and state plainly that streaming adapters are intentionally deferred until real usage proves they are worth the extra surface area. Do not introduce any new default behavior or imply that adapters already exist.</action>
  <verify><automated>go test ./textnorm -count=1</automated></verify>
  <done>Package docs explain how the package is hardened today and where future adapter work stops.</done>
</task>

</tasks>

<verification>
Before declaring the plan complete:
- [ ] `go test ./textnorm -count=1`
- [ ] `go test ./textnorm -run '^$' -bench 'Benchmark(Pipeline|SearchPreset|CanonicalPreset|DBSafePreset)' -benchmem -count=1`
- [ ] `go test ./textnorm -run '^$' -fuzz 'Fuzz(Pipeline|SearchPreset|CanonicalPreset|DBSafePreset)' -fuzztime=1s -count=1`
- [ ] Package docs state that streaming adapters are deferred, not silently introduced
</verification>

<success_criteria>
- Phase 3 measures the current implementation before optimization work starts.
- Fuzz coverage protects the public `textnorm` APIs against malformed input regressions.
- The package docs keep the core library-only boundary explicit and future adapters deferred.
- The phase stays small, deterministic, and free of framework creep.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hardening-and-future-adapters/03-01-SUMMARY.md`
</output>

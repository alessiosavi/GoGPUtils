---
phase: 02-token-pipelines-and-presets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - textnorm/tokens.go
  - textnorm/case.go
  - textnorm/width.go
  - textnorm/tokens_test.go
  - textnorm/case_test.go
  - textnorm/width_test.go
autonomous: true
requirements:
  - UNIC-03
  - UNIC-04
  - TOKN-01
  - TOKN-02
  - TOKN-03
  - TOKN-04
  - PRES-04
must_haves:
  truths:
    - "User can split normalized text into whitespace-stable tokens."
    - "User can map and filter tokens, then join them back with a caller-chosen separator."
    - "User can fold case or lowercase text as part of a normalization pipeline."
    - "User can filter or map runes by class for canonical output."
    - "Width folding stays opt-in and does not change default pipeline behavior."
  artifacts:
    - path: "textnorm/tokens.go"
      provides: "TokenPipeline contracts plus split/map/filter/join token stages"
    - path: "textnorm/case.go"
      provides: "Case-folding, lowercasing, and rune-class transform stages"
    - path: "textnorm/width.go"
      provides: "Opt-in width folding stage built on golang.org/x/text/width"
    - path: "textnorm/tokens_test.go"
      provides: "Token-boundary and join behavior tests"
    - path: "textnorm/case_test.go"
      provides: "Case and rune-class behavior tests"
    - path: "textnorm/width_test.go"
      provides: "Width-folding opt-in tests"
  key_links:
    - from: "textnorm/pipeline.go"
      to: "textnorm/tokens.go"
      via: "Pipeline.SplitTokens() tokenizes the source pipeline output with strings.Fields"
      pattern: "SplitTokens|JoinTokens"
    - from: "textnorm/case.go"
      to: "golang.org/x/text/cases"
      via: "FoldCase() uses cases.Fold and Lower() uses cases.Lower(language.Und)"
      pattern: "cases\.Fold|cases\.Lower"
    - from: "textnorm/case.go"
      to: "golang.org/x/text/runes"
      via: "MapRunes() and FilterRunes() compose rune-class transforms from runes.Map and runes.Predicate"
      pattern: "runes\.(Map|Predicate|Remove)"
    - from: "textnorm/width.go"
      to: "golang.org/x/text/width"
      via: "FoldWidth() applies width.Fold only when explicitly requested"
      pattern: "width\.Fold"
---

<objective>
Build the token-pipeline and normalization-stage foundation that lets callers split, transform, and rejoin normalized text deterministically.

Purpose: Phase 2 needs reusable token and rune transforms before presets can stay thin.
Output: TokenPipeline APIs, case/rune/width stages, and tests proving boundary and transform behavior.
</objective>

<execution_context>
@/Users/alessiosavi/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/alessiosavi/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-pipeline-and-unicode-safety/01-01-SUMMARY.md
@textnorm/pipeline.go
@textnorm/stages.go
@textnorm/unicode.go
@textnorm/whitespace.go

<interfaces>
From textnorm/pipeline.go:
```go
type Pipeline struct {
    stages []Stage
}

func New() Pipeline
func (p Pipeline) Then(stage Stage) Pipeline
func (p Pipeline) Run(input string) (string, error)
```

From textnorm/stages.go:
```go
type Stage func(string) (string, error)
```

From golang.org/x/text/cases:
```go
func Fold(opts ...Option) Caser
func Lower(t language.Tag, opts ...Option) Caser
```

From golang.org/x/text/runes:
```go
type Set interface { Contains(rune) bool }
func Predicate(f func(rune) bool) Set
```

From golang.org/x/text/width:
```go
var Fold Transformer
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add token pipeline contracts and whitespace-first splitting</name>
  <files>textnorm/tokens.go</files>
  <read_first>
    textnorm/pipeline.go
    textnorm/stages.go
    textnorm/whitespace.go
    .planning/phases/01-core-pipeline-and-unicode-safety/01-01-SUMMARY.md
  </read_first>
  <behavior>
    - SplitTokens() must use the source Pipeline output and tokenize with strings.Fields, so whitespace normalization stays the boundary rule.
    - TokenPipeline must support map, filter, and join steps without mutating prior values.
    - JoinTokens(sep) must return a string Pipeline stage so token pipelines can re-enter the string pipeline cleanly.
  </behavior>
  <action>
    Create a TokenPipeline API that mirrors the existing immutable Pipeline style. Add a token-stage contract, a SplitTokens() entry point on Pipeline, map/filter helpers, Run(input string) ([]string, error), and JoinTokens(sep string) Pipeline. Keep zero values valid, preserve declaration order, and make whitespace splitting the only token-boundary strategy in this task.
  </action>
  <acceptance_criteria>
    - textnorm/tokens.go contains exported TokenPipeline and TokenStage types.
    - textnorm/pipeline.go gains SplitTokens() via methods in the new file.
    - Token splitting uses strings.Fields on the source pipeline output.
    - JoinTokens joins with the caller-supplied separator using strings.Join.
  </acceptance_criteria>
  <verify>
    <automated>go test ./textnorm -count=1</automated>
  </verify>
  <done>Token pipelines can split, transform, and rejoin text without changing the immutable string-pipeline model.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add case folding, rune-class transforms, and width folding</name>
  <files>textnorm/case.go,textnorm/width.go</files>
  <read_first>
    textnorm/pipeline.go
    textnorm/unicode.go
    textnorm/whitespace.go
    go.mod
  </read_first>
  <behavior>
    - FoldCase() must use golang.org/x/text/cases.Fold for Unicode case folding.
    - Lower() must use golang.org/x/text/cases.Lower(language.Und) for explicit lowercasing.
    - MapRunes() and FilterRunes() must work as pipeline stages so canonical output can keep or rewrite rune classes.
    - FoldWidth() must apply golang.org/x/text/width.Fold only when explicitly appended.
  </behavior>
  <action>
    Add explicit string-pipeline stages for Unicode-aware case folding/lowercasing, rune-class filtering, rune mapping, and opt-in width folding. Use cases.Fold, cases.Lower(language.Und), runes.Predicate, runes.Remove, runes.Map, and width.Fold directly; do not hide any of these transforms behind presets yet. Keep every stage additive and deterministic.
  </action>
  <acceptance_criteria>
    - textnorm/case.go exports FoldCase(), Lower(), MapRunes(), and FilterRunes().
    - textnorm/width.go exports FoldWidth().
    - The default Pipeline path is unchanged unless one of the new stages is chained.
    - Width folding is not enabled implicitly by any constructor or existing stage.
  </acceptance_criteria>
  <verify>
    <automated>go test ./textnorm -count=1</automated>
  </verify>
  <done>Unicode-aware case, rune-class, and width transforms are available as explicit pipeline stages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add token and stage regression tests</name>
  <files>textnorm/tokens_test.go,textnorm/case_test.go,textnorm/width_test.go</files>
  <read_first>
    textnorm/tokens.go
    textnorm/case.go
    textnorm/width.go
    textnorm/pipeline_test.go
  </read_first>
  <behavior>
    - Token tests must prove whitespace-first splitting, token mapping, filtering, and joining behave deterministically.
    - Case tests must distinguish FoldCase() from Lower() and verify rune-class transforms on mixed Unicode input.
    - Width tests must prove width folding changes only explicit opt-in pipelines.
  </behavior>
  <action>
    Add table-driven tests for the new token pipeline and normalization stages. Cover boundary cases with repeated whitespace, mixed Unicode case, rune-class filtering, and mixed-width text. Use direct equality checks and slices.Equal where token slices are compared.
  </action>
  <acceptance_criteria>
    - tests cover SplitTokens(), MapTokens(), FilterTokens(), JoinTokens(), FoldCase(), Lower(), MapRunes(), FilterRunes(), and FoldWidth().
    - at least one test proves width folding is absent unless FoldWidth() is chained.
    - at least one test proves token boundaries come from whitespace normalization, not punctuation stripping.
  </acceptance_criteria>
  <verify>
    <automated>go test ./textnorm -run 'Test(TokenPipeline|Case|Width)' -count=1</automated>
  </verify>
  <done>All phase 2 stage primitives are covered by regression tests.</done>
</task>

</tasks>

<verification>
Run the textnorm package tests after each task, then run the full repository suite once all three tasks are complete. The wave is only complete when all new stages compile and the token/case/width tests pass.
</verification>

<success_criteria>
1. Token pipelines can split, transform, and rejoin normalized text deterministically.
2. Case folding, lowercasing, rune-class transforms, and width folding exist as explicit opt-in stages.
3. The new stage tests pass without changing existing phase 1 behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/02-token-pipelines-and-presets/02-01-SUMMARY.md`.
</output>

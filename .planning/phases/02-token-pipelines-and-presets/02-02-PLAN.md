---
phase: 02-token-pipelines-and-presets
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - textnorm/presets.go
  - textnorm/presets_test.go
  - textnorm/doc.go
autonomous: true
requirements:
  - PRES-01
  - PRES-02
  - PRES-03
must_haves:
  truths:
    - "User can call search, canonical, and DB-safe presets without assembling stages manually."
    - "Width folding stays opt-in and does not change default preset behavior."
    - "Presets reuse the stage API from wave 1 instead of duplicating cleanup logic."
    - "Package docs explain what each preset does and when width folding should be enabled."
  artifacts:
    - path: "textnorm/presets.go"
      provides: "Preset constructors and width-fold option wiring"
    - path: "textnorm/presets_test.go"
      provides: "Preset parity and opt-in behavior tests"
    - path: "textnorm/doc.go"
      provides: "Public package overview and usage examples for presets"
  key_links:
    - from: "textnorm/presets.go"
      to: "textnorm/tokens.go"
      via: "SearchPreset() uses SplitTokens() and JoinTokens() to keep token handling explicit"
      pattern: "SplitTokens|JoinTokens"
    - from: "textnorm/presets.go"
      to: "textnorm/case.go"
      via: "Presets compose FoldCase(), Lower(), MapRunes(), and FilterRunes() rather than reimplementing transforms"
      pattern: "FoldCase|Lower|MapRunes|FilterRunes"
    - from: "textnorm/presets.go"
      to: "textnorm/width.go"
      via: "WithWidthFold() toggles FoldWidth() only when requested"
      pattern: "WithWidthFold|FoldWidth"
---

<objective>
Turn the new token and normalization stages into thin, reusable presets for search, canonical, and DB-safe text handling.

Purpose: Users should be able to choose a preset instead of assembling common pipelines by hand.
Output: Preset constructors, preset docs, and tests proving the default behavior stays explicit.
</objective>

<execution_context>
@/Users/alessiosavi/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/alessiosavi/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-pipeline-and-unicode-safety/01-01-SUMMARY.md
@.planning/phases/02-token-pipelines-and-presets/02-01-PLAN.md
@textnorm/pipeline.go
@textnorm/tokens.go
@textnorm/case.go
@textnorm/width.go
@textnorm/unicode.go
@textnorm/whitespace.go

<interfaces>
From wave 1 outputs in textnorm/tokens.go, textnorm/case.go, and textnorm/width.go:
```go
func (p Pipeline) SplitTokens() TokenPipeline
func (tp TokenPipeline) MapTokens(fn func(string) string) TokenPipeline
func (tp TokenPipeline) FilterTokens(fn func(string) bool) TokenPipeline
func (tp TokenPipeline) JoinTokens(sep string) Pipeline

func (p Pipeline) FoldCase() Pipeline
func (p Pipeline) Lower() Pipeline
func (p Pipeline) MapRunes(fn func(rune) rune) Pipeline
func (p Pipeline) FilterRunes(keep runes.Set) Pipeline
func (p Pipeline) FoldWidth() Pipeline
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add preset constructors and width-fold option wiring</name>
  <files>textnorm/presets.go</files>
  <read_first>
    textnorm/pipeline.go
    textnorm/tokens.go
    textnorm/case.go
    textnorm/width.go
    textnorm/unicode.go
    textnorm/whitespace.go
  </read_first>
  <behavior>
    - SearchPreset() must build a search-key pipeline that stays thin and reuses existing stages.
    - CanonicalPreset() must normalize text without tokenizing unless the stage contract requires it.
    - DBSafePreset() must keep UTF-8/NUL sanitization explicit and stable for persistence.
    - WithWidthFold() must toggle width folding in every preset without affecting defaults.
  </behavior>
  <action>
    Add preset builder functions that compose wave-1 stages instead of introducing new normalization logic. Create a small presetConfig plus a WithWidthFold() option. Implement SearchPreset(opts ...PresetOption), CanonicalPreset(opts ...PresetOption), and DBSafePreset(opts ...PresetOption) so each one can insert FoldWidth() when WithWidthFold() is passed, but leaves the default path unchanged. Keep the concrete stage order visible in the source: search = NormalizeUnicode -> FoldCase -> rune-class filtering -> TrimSpace -> CollapseWhitespace -> SplitTokens -> JoinTokens(" "); canonical = NormalizeUnicode -> FoldCase -> TrimSpace -> CollapseWhitespace; db-safe = SanitizeUTF8 -> NormalizeUnicode -> TrimSpace -> CollapseWhitespace.
  </action>
  <acceptance_criteria>
    - textnorm/presets.go exports SearchPreset(), CanonicalPreset(), DBSafePreset(), and WithWidthFold().
    - all three presets are built from existing stage methods; no duplicated transform loops appear in the file.
    - width folding is opt-in via the option, not enabled by default.
    - search preset returns a single normalized string suitable for search/index keys.
  </acceptance_criteria>
  <verify>
    <automated>go test ./textnorm -count=1</automated>
  </verify>
  <done>Preset constructors are available and reuse the phase-2 stage API.</done>
</task>

<task type="auto">
  <name>Task 2: Document the preset surface and usage examples</name>
  <files>textnorm/doc.go</files>
  <read_first>
    textnorm/presets.go
    textnorm/tokens.go
    textnorm/case.go
    textnorm/width.go
  </read_first>
  <action>
    Rewrite the package doc to describe the new token pipeline and the three presets. Add short examples that show SearchPreset(), CanonicalPreset(), DBSafePreset(), and WithWidthFold() in use. Make the docs explicit about what each preset does and that width folding remains opt-in.
  </action>
  <acceptance_criteria>
    - textnorm/doc.go mentions SplitTokens(), MapTokens(), FilterTokens(), JoinTokens(), SearchPreset(), CanonicalPreset(), DBSafePreset(), and WithWidthFold().
    - the examples describe search, canonical, and DB-safe behavior in plain language.
    - no new hidden defaults are introduced in the prose.
  </acceptance_criteria>
  <verify>
    <automated>go test ./textnorm -count=1</automated>
  </verify>
  <done>Package documentation matches the exported preset API and its opt-in width-fold behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add preset regression tests</name>
  <files>textnorm/presets_test.go</files>
  <read_first>
    textnorm/presets.go
    textnorm/tokens.go
    textnorm/case.go
    textnorm/width.go
    textnorm/unicode.go
    textnorm/whitespace.go
    textnorm/pipeline_test.go
  </read_first>
  <behavior>
    - SearchPreset() must prove stable search-key output on mixed Unicode input.
    - CanonicalPreset() must prove punctuation is preserved unless a filter is explicitly added.
    - DBSafePreset() must prove invalid UTF-8 and NUL bytes are sanitized.
    - WithWidthFold() must prove mixed-width text changes only when the option is supplied.
  </behavior>
  <action>
    Add table-driven tests for the three presets and the width option. Check that the presets stay thin by asserting their outputs come from the same stage behaviors introduced in wave 1, and verify that width folding is absent from the default preset output but present when requested.
  </action>
  <acceptance_criteria>
    - tests include TestSearchPreset, TestCanonicalPreset, TestDBSafePreset, and TestWidthFoldOption.
    - at least one test shows the search preset removes punctuation and collapses whitespace.
    - at least one test shows the DB-safe preset preserves valid text while sanitizing bad UTF-8/NUL input.
    - at least one test shows width folding does not alter output unless WithWidthFold() is passed.
  </acceptance_criteria>
  <verify>
    <automated>go test ./textnorm -run 'Test(SearchPreset|CanonicalPreset|DBSafePreset|WidthFoldOption)' -count=1</automated>
  </verify>
  <done>Preset behavior is regression-tested and width folding remains explicitly opt-in.</done>
</task>

</tasks>

<verification>
Confirm the preset constructors compile against wave 1, the doc file explains the public surface, and the preset tests pass. Phase 2 is complete only when search, canonical, and DB-safe outputs are deterministic and width folding remains opt-in.
</verification>

<success_criteria>
1. Callers can use search, canonical, and DB-safe presets instead of assembling common pipelines manually.
2. Presets reuse the stage API from wave 1 rather than duplicating cleanup logic.
3. Width folding is opt-in and does not affect default behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/02-token-pipelines-and-presets/02-02-SUMMARY.md`.
</output>

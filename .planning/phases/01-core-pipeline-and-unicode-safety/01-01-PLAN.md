---
phase: 01-core-pipeline-and-unicode-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - textnorm/doc.go
  - textnorm/pipeline.go
  - textnorm/stages.go
  - textnorm/unicode.go
  - textnorm/whitespace.go
  - textnorm/pipeline_test.go
  - textnorm/unicode_test.go
  - textnorm/whitespace_test.go
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
  - UNIC-01
  - UNIC-02
must_haves:
  truths:
    - "Pipeline stages execute strictly left-to-right and preserve declaration order."
    - "Pipeline values are immutable: chaining a stage returns a new value and leaves the original unchanged."
    - "Unicode normalization removes combining marks for accent-insensitive output."
    - "Whitespace cleanup and UTF-8/NUL sanitization are available as explicit stages."
  artifacts:
    - "textnorm/doc.go"
    - "textnorm/pipeline.go"
    - "textnorm/stages.go"
    - "textnorm/unicode.go"
    - "textnorm/whitespace.go"
    - "textnorm/*_test.go"
  key_links:
    - "`Pipeline` owns ordered stage execution and exposes the fluent chain API."
    - "Unicode cleanup stages wrap `golang.org/x/text/transform`, `unicode/norm`, and `runes`."
    - "Whitespace and sanitization stages operate on the same pipeline contract as Unicode stages."
---

<objective>
Establish the core `textnorm` package as a small, immutable, fluent pipeline for deterministic text normalization.

Purpose: Create the execution model and Unicode-safe cleanup foundation that every later stage depends on.
Output: A new `textnorm` package with pipeline primitives, Unicode cleanup stages, whitespace/UTF-8 sanitization stages, and tests that prove deterministic behavior.
</objective>

<execution_context>
@/Users/alessiosavi/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/alessiosavi/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@go.mod
@stringutil/clean.go
@stringutil/stringutil.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the immutable pipeline core</name>
  <files>textnorm/doc.go, textnorm/pipeline.go, textnorm/stages.go, textnorm/pipeline_test.go</files>
  <read_first>textnorm/doc.go, textnorm/pipeline.go, textnorm/stages.go, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, stringutil/clean.go, stringutil/stringutil.go, go.mod</read_first>
  <action>Create a new top-level `textnorm` package that does not import `stringutil`. Define a small immutable pipeline API with `type Stage func(string) (string, error)`, `type Pipeline struct`, `func New() Pipeline`, `func (p Pipeline) Then(stage Stage) Pipeline`, and `func (p Pipeline) Run(input string) (string, error)`. Keep the zero-value pipeline valid, preserve stage order exactly as declared, and return a new pipeline value from every chaining method. Add package docs that describe the package as a fluent text-normalization pipeline for search/DB canonicalization and scraping cleanup.</action>
  <verify>go test ./textnorm -count=1</verify>
  <acceptance_criteria>
    - `textnorm/pipeline.go` contains `type Stage func(string) (string, error)`.
    - `textnorm/pipeline.go` contains `func New() Pipeline`.
    - `textnorm/pipeline.go` contains `func (p Pipeline) Then(stage Stage) Pipeline`.
    - `textnorm/pipeline.go` contains `func (p Pipeline) Run(input string) (string, error)`.
    - `textnorm/pipeline_test.go` contains a test proving chaining does not mutate the original pipeline.
    - `go test ./textnorm -count=1` exits 0.
  </acceptance_criteria>
  <done>The pipeline core exists, is immutable, and has tests for order/determinism.</done>
</task>

<task type="auto">
  <name>Task 2: Add Unicode and whitespace cleanup stages</name>
  <files>textnorm/unicode.go, textnorm/whitespace.go, textnorm/unicode_test.go, textnorm/whitespace_test.go</files>
  <read_first>textnorm/pipeline.go, textnorm/unicode.go, textnorm/whitespace.go, .planning/REQUIREMENTS.md, .planning/research/SUMMARY.md, go.mod, stringutil/clean.go</read_first>
  <action>Implement explicit cleanup stages on `Pipeline` for `NormalizeUnicode()`, `RemoveAccents()`, `TrimSpace()`, `CollapseWhitespace()`, and `SanitizeUTF8()`. Use `golang.org/x/text/transform`, `golang.org/x/text/unicode/norm`, and `golang.org/x/text/runes` for Unicode decomposition plus combining-mark removal, and use stdlib `strings` and `unicode/utf8` for whitespace collapse and invalid UTF-8/NUL sanitization. Keep the stages explicit and deterministic; do not add tokenization, transliteration, or HTML parsing in Phase 1.</action>
  <verify>go test ./textnorm -count=1</verify>
  <acceptance_criteria>
    - `textnorm/unicode.go` contains a `transform.Chain` using `norm.NFKD`, `runes.Remove`, and `norm.NFC`.
    - `textnorm/whitespace.go` contains explicit whitespace cleanup and UTF-8/NUL sanitization logic.
    - `textnorm/unicode_test.go` includes a fixture showing `café` normalizes to `cafe`.
    - `textnorm/whitespace_test.go` includes a fixture showing invalid UTF-8 or NUL bytes are sanitized.
    - `go test ./textnorm -count=1` exits 0.
  </acceptance_criteria>
  <done>Unicode normalization, accent removal, and whitespace/UTF-8 cleanup are available as reusable pipeline stages.</done>
</task>

</tasks>

<verification>
Before declaring the plan complete:
- [ ] `go test ./textnorm -count=1`
- [ ] The package docs describe `textnorm` as a new fluent pipeline package
- [ ] The stage order is deterministic and immutable
- [ ] Unicode normalization uses `golang.org/x/text`
</verification>

<success_criteria>
- The `textnorm` package exists and is the canonical normalization entry point for this project.
- Phase 1 requirements PIPE-01, PIPE-02, PIPE-03, UNIC-01, and UNIC-02 are covered by the plan.
- The plan is fully autonomous and contains no checkpoints.
- The implementation remains small, deterministic, and library-only.
</success_criteria>
